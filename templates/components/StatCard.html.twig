<div class="bg-white p-6 rounded-2xl border border-gray-200 shadow-sm flex flex-col gap-4">
    <div class="w-10 h-10 rounded-lg flex items-center justify-center {{ color }}">
        <i class="{{ icon }} text-lg"></i>
    </div>
    <div>
        <p class="text-[13px] text-gray-500 mb-1">{{ label }}</p>
        <p class="text-2xl font-bold text-gray-900">{{ value }}</p>
    </div>
</div>