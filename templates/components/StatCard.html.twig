<div class="bg-white p-5 sm:p-6 rounded-2xl border border-gray-200 shadow-sm flex flex-col gap-3 sm:gap-4">
    <div class="w-8 h-8 sm:w-10 sm:h-10 rounded-lg flex items-center justify-center {{ color }}">
        <i class="{{ icon }} text-base sm:text-lg"></i>
    </div>
    <div>
        <p class="text-[12px] sm:text-[13px] text-gray-500 mb-1">{{ label }}</p>
        <p class="text-xl sm:text-2xl font-bold text-gray-900">{{ value }}</p>
    </div>
</div>